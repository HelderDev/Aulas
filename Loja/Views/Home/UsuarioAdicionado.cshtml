@model IEnumerable<Loja.Models.Usuario>

@{
	ViewBag.Title = "UsuarioAdicionado";
}

<h2>Usuario Adicionado</h2>
@for (int i = 0; i < Model.Count(); i++)
{
	@Html.DisplayFor(m => Model.ElementAt(i).Nome)

	@Html.DisplayFor(m => Model.ElementAt(i).Funcao)
	<br>
}